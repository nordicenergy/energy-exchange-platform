NavigationCard component:

```jsx
const wrapperStyles = {
    display: 'flex',
    justifyContent: 'center',
    padding: '1rem',
    backgroundColor: '#e6e6e6'
};
<div style={wrapperStyles}>
    <NavigationCard type="my_producer" title="My producer" onCardClickHandler={() => {}} />
</div>;
```
